{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../../Part_2/part2/examples && npm run build && cp -r dist /c/Users/D/Documents/Annika/Coding/FullStack_Helsinki/Part_3/part3/examples",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Annika Alonzo",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
